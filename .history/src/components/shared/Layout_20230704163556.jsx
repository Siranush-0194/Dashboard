import React from "react";
import { Outlet } from "react-router-dom";

export default function Layout() {
    <div>
        <div>
            sidebar
        </div>
        <div>headder</div>
        {/* <div>{<Outlet />}</div> */}
    </div>
}